<div class="messages">
	<% messages.forEach(function(type) { %>
	    <%= messages.type.forEach(function(message) { %>
	        <div class='alert alert-danger'><%= message %></div>
	    <% }); %>
	<% }); %>
</div>
